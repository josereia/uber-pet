#ifndef utils
#define utils

#include <string>
#include <vector>

namespace Utils {
using namespace std;

vector<vector<string>> filter(vector<vector<string>> data, string filter);
}  // namespace Utils

#endif  // utils
